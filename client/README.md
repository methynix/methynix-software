# Methynix — website (client)

React + Vite frontend. See `../SETUP.md` for full setup, deployment, and the list of things you need to fill in.

Quick start:

```
npm install
cp .env.example .env
npm run dev
```
